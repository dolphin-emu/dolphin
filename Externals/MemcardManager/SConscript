# -*- python -*-

Import('env')

if not env['HAVE_WX']:
	Return()

files = [
	'src/mcmMain.cpp',
	'src/MCMdebug.cpp',
	]

libs = [
	'memcard',
	'common',
	]

env.Program('MemcardManager', files, LIBS = env['LIBS'] + libs)
