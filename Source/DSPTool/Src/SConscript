# -*- python -*-

Import('env')
import sys

dtenv = env.Clone()

files = [
    'main.cpp',
    ]

libs = [
    'dspcore',
    'common',
]

dtenv.Append(LIBS = libs)

dtenv.Program(dtenv['binary_dir'] + 'dsptool', files)
