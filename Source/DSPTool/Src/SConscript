# -*- python -*-

Import('env')
import sys

files = [
	'DSPTool.cpp',
	]

libs = [
	env['common'],
	env['dspcore'],
	]

if sys.platform == 'darwin':
	frames = ['CoreFoundation']
else:
	frames = []

env.Program('dsptool', files, LIBS = libs, FRAMEWORKS = frames)
