# -*- python -*-

Import('env')

dtenv = env.Clone()

files = [
    'main.cpp',
    ]

libs = [
    'dspcore',
    'common',
]


dtenv.Append(CXXFLAGS = [ '-fPIC' ],
             LIBS = libs)

dtenv.Program(dtenv['binary_dir'] + 'dsptool', files)
