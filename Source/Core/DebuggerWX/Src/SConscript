# -*- python -*-

Import('env')

if not env['HAVE_WX']:
    Return()

files = [
    "BreakPointDlg.cpp",
    "BreakpointView.cpp",
    "CodeView.cpp",
    "BreakpointWindow.cpp",
    "CodeWindow.cpp",
    "CodeWindowSJP.cpp",
    "CodeView.cpp",
    "MemoryCheckDlg.cpp",
    "MemoryView.cpp",
    "MemoryWindow.cpp",
    "RegisterWindow.cpp",
    "RegisterView.cpp",
    "JitWindow.cpp",
    ]
wxenv = env.Clone()
wxenv.Append(
	CPPDEFINES = [
		'USE_XPM_BITMAPS',
		'wxNEEDS_CHARPP'
		],
	LINKFLAGS = [
		'-pthread',
		]
	)
libs = [
    'common'
]

wxenv.StaticLibrary(env['local_libs'] + "debwx", files, LIBS = libs)
